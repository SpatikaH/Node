/**
 * @file Declares Tag representing the tags associated with a tuit
 */
/**
 * @typedef Tag Represents tags associated with a tuit
 * @property {string} tag is the string associated with a tuit
 */
export default class Tag {
    private tag: string = '';
};